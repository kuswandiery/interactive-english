export interface LearningGoal {
  value: string
  label: string
}

export interface StudentProfile {
  name: string
  email: string
  bio: string
  goal: string
  preferredLanguage: string
  avatar: string
}